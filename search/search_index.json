{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Upsight","text":"<p>End-user documentation for Upsight, the desktop app for technical CSMs at Kong.</p> <p>Upsight keeps your account data, meeting notes, win wires, and external dashboards in one place. Everything is stored locally and works offline. Data syncs to and from Salesforce on demand.</p>"},{"location":"#sections","title":"Sections","text":"<ul> <li>Getting started, first launch, navigation, and basic setup</li> <li>Install on macOS, download the <code>.dmg</code>, get past Gatekeeper, and install runtime dependencies</li> <li>Install on NixOS, add the flake and install the package</li> <li>Accounts, what accounts are and how to work with them</li> <li>Salesforce integration, authentication, sync, and write-back</li> <li>Meeting summaries, AI summaries and the <code>upsight summarize</code> CLI</li> <li>Monthly CX update, the monthly update workflow and due-date reminder</li> <li>Dashboards, add and open external dashboard links</li> <li>Backup and restore, automatic backups, manual backup, and restoring</li> <li>Multi-machine sync, using Syncthing to share data across machines</li> <li>Settings, a reference for every settings option</li> </ul>"},{"location":"accounts/","title":"Accounts","text":"<p>Each account in Upsight corresponds to a customer organization from Salesforce. Accounts are the primary unit of work. Most of the app's screens live inside an account context.</p>"},{"location":"accounts/#the-accounts-list","title":"The accounts list","text":"<p>The Home screen shows every account you have synced from Salesforce. Click any row to open that account. The search bar at the top filters by name.</p> <p>Each row shows the account name and a summary of health indicators from the most recent Salesforce sync.</p>"},{"location":"accounts/#sections-within-an-account","title":"Sections within an account","text":"<p>Once you open an account, the sidebar expands with these tabs:</p> <ul> <li>Overview shows the at-a-glance summary: consumption gauge, health posture,   recent activity, and upcoming meetings.</li> <li>Health shows posture indicators and enrichment status from Salesforce, with   inline editing. Field edits save locally and queue for write-back if Salesforce   write-back is enabled.</li> <li>People lists contacts associated with the account, pulled from Salesforce.</li> <li>Action Items tracks open tasks for this account.</li> <li>Meetings shows past and upcoming meeting records, with AI-generated summaries   where available.</li> <li>Agendas holds meeting agenda drafts you can copy or share.</li> <li>CX Events shows customer experience events from Salesforce.</li> <li>Opportunities lists open and recently closed Salesforce opportunities.</li> <li>Win Wires holds win wire documents. New entries are pulled from Salesforce   and PDFs can be attached.</li> <li>Stack shows the customer's technical stack: Konnect orgs, installed products   and plugins, and infrastructure details. You maintain these manually.</li> <li>Usage displays product utilization from account entitlements data.</li> <li>Q&amp;A stores a knowledge base of questions and answers you have built for the   account.</li> <li>Documents attaches arbitrary files to the account (PDFs, notes, and similar).</li> <li>Resources holds links and references specific to the account.</li> </ul>"},{"location":"accounts/#syncing-an-account","title":"Syncing an account","text":"<p>Upsight pulls Salesforce data on demand, not on a continuous schedule. Click the sync button in the account header (the rotating-arrows icon above the tab strip) to pull the latest data. The app reads from your Salesforce CLI session (<code>sf</code>) and updates the local database.</p> <p>Edits you make in the app (posture fields, notes, health indicators) save locally immediately. If Salesforce write-back is enabled, the app pushes those changes back to Salesforce in the background. See Salesforce integration for details.</p>"},{"location":"backup-restore/","title":"Backup and restore","text":"<p>Upsight maintains automatic rolling backups of your database. Each backup is a single archive containing the SQLite database and your account files (documents, win wires, logos).</p>"},{"location":"backup-restore/#automatic-backups","title":"Automatic backups","text":"<p>By default the app takes a backup every 24 hours and keeps:</p> <ul> <li>7 daily snapshots</li> <li>4 weekly snapshots</li> </ul> <p>When the retention limits are reached, older snapshots are removed automatically.</p> <p>You can adjust the schedule and retention in Settings under Data, \"Automatic backups.\" Changes take effect on the next app launch.</p> <p>To see when the last backup ran, go to Settings, then About. The Diagnostics panel shows the backup directory path and the timestamp of the last backup.</p>"},{"location":"backup-restore/#manual-backup","title":"Manual backup","text":"<p>To take a backup immediately, go to Settings, then Data, and click \"Back up now.\" Or go to Settings, About, Diagnostics, and click \"Back up now.\" The app reports the filename when the backup completes.</p>"},{"location":"backup-restore/#backup-file-location","title":"Backup file location","text":"<p>Upsight stores backups in a subdirectory of its data directory. The exact path is shown in Settings under About, Diagnostics, Backups.</p>"},{"location":"backup-restore/#restoring-from-a-backup","title":"Restoring from a backup","text":"<p>To restore, close the app and replace the database file with the backup copy.</p> <p>The database file path is shown in Settings under About, Diagnostics, Database. It is also printed in Settings under Data, Database, \"Path.\"</p> <p>Steps to restore:</p> <ol> <li>Close Upsight.</li> <li>Find the backup file you want to restore from (in the backups directory shown    in Settings).</li> <li>Copy the backup file over the current database file, overwriting it.</li> <li>Reopen Upsight.</li> </ol> <p>Backup filenames include a timestamp so you can identify which snapshot to use.</p> <p>If you have Syncthing running, pause the sync before restoring so Syncthing does not conflict with the file operation. Resume sync after the app starts and confirms the database opens cleanly.</p>"},{"location":"backup-restore/#compacting-the-database","title":"Compacting the database","text":"<p>The \"Compact (VACUUM)\" button in Settings under Data runs SQLite's VACUUM command on the database. This rewrites the file to recover space from deleted rows and fragmentation. It is safe to run anytime, but it takes a few seconds for large databases. It also requires exclusive access, so the app must be the only process with the file open.</p>"},{"location":"backup-restore/#import-and-export","title":"Import and export","text":"<p>You can move account data between Upsight instances using the import and export tools in Settings under Data, \"Import / Export.\"</p> <p>Exporting produces a ZIP bundle with the selected accounts' full dataset, including contacts, opportunities, interactions, win wires, cases, tasks, meeting history, and attachments.</p> <p>Importing reads a bundle and merges it into the current database. If the bundle contains accounts that already exist (matched by Salesforce ID), the import asks whether to skip those accounts or update them.</p>"},{"location":"dashboards/","title":"Dashboards","text":"<p>The Dashboards screen is a global shortcut list for external URLs you check regularly: Looker reports, Grafana boards, Salesforce list views, and anything else with a URL. Adding a link here means you can reach it in one click without hunting through browser bookmarks.</p> <p>Dashboards are not account-scoped. The list applies across all accounts.</p>"},{"location":"dashboards/#adding-a-dashboard","title":"Adding a dashboard","text":"<p>Click \"Add Dashboard\" at the top right of the Dashboards screen. Enter a name and a URL. You can optionally add an icon to make the entry easy to spot in the list.</p> <p>Save the entry. It appears in the list immediately and stays there across app restarts.</p>"},{"location":"dashboards/#opening-a-dashboard","title":"Opening a dashboard","text":"<p>Click any entry in the list to open it in your default browser (the app uses your system's default URL opener).</p>"},{"location":"dashboards/#editing-and-removing-entries","title":"Editing and removing entries","text":"<p>Each entry has an edit button on the right side of its row. Click it to change the name, URL, or icon, or to remove the entry.</p>"},{"location":"dashboards/#reordering","title":"Reordering","text":"<p>Use the up and down arrows on each row to reorder the list. The order is saved automatically.</p>"},{"location":"dashboards/#jumping-to-a-dashboard-by-name","title":"Jumping to a dashboard by name","text":"<p>Use <code>Ctrl+K</code> to open the command palette. Type any part of a dashboard name to find and open it directly, without navigating to the Dashboards screen first.</p>"},{"location":"getting-started/","title":"Getting started","text":"<p>Upsight is a desktop app for technical CSMs at Kong. It keeps your account data, meeting notes, win wires, and external dashboards in one place, all local and offline-capable, synced to Salesforce on demand.</p>"},{"location":"getting-started/#first-launch","title":"First launch","text":"<p>The app opens to the accounts list (Home). On first launch this list is empty. Accounts are pulled from Salesforce the first time you authenticate and run a sync.</p> <p>Before syncing, you need:</p> <ul> <li>The Salesforce CLI (<code>sf</code>) installed and authenticated: <code>sf org login web</code>. The   app uses your existing CLI session and never handles your Salesforce credentials   directly.</li> <li>An AI binary for AI features: the <code>claude</code> CLI on <code>PATH</code> and logged in.   You can skip this and add it later under Settings.</li> </ul>"},{"location":"getting-started/#navigating-the-app","title":"Navigating the app","text":"<p>The left sidebar has two levels.</p> <p>The top section (Home, Action Items, Cases, Feature Requests, Dashboards, Events) is global. These show data across all accounts.</p> <p>When you select an account from the Home list, the sidebar adds an account-scoped section below the global items. This contains tabs for that account: Overview, Health, People, Action Items, Meetings, Agendas, CX Events, Opportunities, Win Wires, Stack, Usage, Q&amp;A, Documents, and Resources.</p> <p>Click a different account in the Home list to switch. The account-scoped section updates to reflect the new selection.</p>"},{"location":"getting-started/#keyboard-shortcuts","title":"Keyboard shortcuts","text":"Shortcut Action <code>Ctrl+1</code> Home (accounts list) <code>Ctrl+2</code> Cases <code>Ctrl+3</code> Settings <code>Ctrl+4</code> Feature Requests <code>Ctrl+5</code> Dashboards <code>Ctrl+K</code> Command palette (jump anywhere by name)"},{"location":"getting-started/#density-and-theme","title":"Density and theme","text":"<p>You can switch between light and dark themes (or follow your system setting) and between comfortable and compact row density in Settings under General.</p>"},{"location":"getting-started/#where-data-lives","title":"Where data lives","text":"<p>Everything is stored in a single SQLite file on your machine. The installed app (Nix-packaged on Linux, or the macOS <code>.dmg</code> release) uses <code>~/.local/share/upsight/upsight.db</code> by default. Set <code>UPSIGHT_DB</code> before launching to point at a different file. The database isn't encrypted at rest, so consider full-disk encryption on your machine, or point <code>UPSIGHT_DB</code> at an encrypted volume.</p> <p>See Backup and restore and Multi-machine sync for how to protect and share that file.</p>"},{"location":"install-macos/","title":"Install on macOS","text":"<p>Upsight publishes a macOS <code>.dmg</code> on every GitHub release. This is the path for anyone who is not building the app from source with Nix. See Install on NixOS if you are on NixOS instead.</p>"},{"location":"install-macos/#download-and-install","title":"Download and install","text":"<ol> <li>Download <code>Upsight.dmg</code> from the    latest release.</li> <li>Open it and drag Upsight into Applications.</li> <li>Launch it from Launchpad or Spotlight.</li> </ol>"},{"location":"install-macos/#getting-past-gatekeeper","title":"Getting past Gatekeeper","text":"<p>The build is unsigned. There is no Apple Developer ID certificate behind it, so macOS Gatekeeper blocks a plain double-click on first launch with an \"Upsight can't be opened because Apple cannot check it for malicious software\" (or \"is damaged and can't be opened\") message. Two ways past it, once:</p> <ul> <li>Right-click (or Control-click) <code>Upsight.app</code> in Applications and choose   Open. Confirm in the dialog that appears. This is the standard macOS path   for unsigned software and only needs doing once per machine.</li> <li>Or, from a terminal: <pre><code>xattr -cr /Applications/Upsight.app\n</code></pre>   This clears the quarantine attribute Gatekeeper checks. Use it if the   right-click path does not offer an Open option.</li> </ul>"},{"location":"install-macos/#runtime-dependencies","title":"Runtime dependencies","text":"<p>The app itself needs nothing beyond the <code>.dmg</code>. A handful of features shell out to external CLIs and need those on <code>PATH</code> separately. Install only what you use, the app runs fine without the rest, and a feature missing its CLI just no-ops (<code>upsight doctor</code>, see below, tells you exactly what is missing).</p> Feature Dependency Install Win-wire PDF text extraction <code>pdftotext</code> (poppler) <code>brew install poppler</code> AI features (meeting summaries, CX indicators, agenda enhancement, \"Investigate\" on a failed write-back) the <code>claude</code> CLI <code>curl -fsSL https://claude.ai/install.sh \\| bash</code>, then <code>claude login</code> Salesforce integration (account/case sync, optional write-back) the <code>sf</code> CLI <code>npm install -g @salesforce/cli</code>, then <code>sf org login web</code> Tableau dashboards surfaced to AI generation (optional, only if configured in Settings) <code>npx</code> running <code>@tableau/mcp-server</code> <code>brew install node</code> (gives you <code>npx</code>), then <code>npm install -g @tableau/mcp-server@2.21.1</code> to seed the cache <p>A few notes on those:</p> <ul> <li>Clipboard copy and desktop notifications need nothing. macOS's   built-in <code>pbcopy</code>/<code>pbpaste</code> and <code>osascript</code> are used automatically.</li> <li>The Salesforce CLI is deliberately installed via <code>npm</code>, not Homebrew.   Homebrew's <code>salesforce-cli</code> cask is deprecated upstream and fails the   Gatekeeper check; <code>npm</code> is Salesforce's own documented install path (and the   one <code>sf update</code> expects). If you don't already have Node, get it first:   <code>brew install node</code>.</li> <li>The AI binary is configurable. <code>claude</code> is the default, but any CLI that   reads a prompt on stdin and writes to stdout works. See   Settings and the <code>[ai]</code> section of <code>config.toml</code> (<code>binary</code> /   <code>args</code>) to point at a different one (for example the Gemini CLI).</li> <li>Tableau MCP is seeded, not just installed. The app invokes it as   <code>npx -y --prefer-offline @tableau/mcp-server@2.21.1</code> at AI-generation time.   <code>--prefer-offline</code> means it resolves from npm's local cache rather than   hitting the network on every AI call, so the one-time global install above   is what makes that fast and avoids a network round-trip, rather than   strictly required. <code>npx</code> would still fetch it fresh each time without it.</li> <li>After installing what you need, run <code>upsight doctor</code> from a terminal (or the   Doctor panel in Settings) to confirm everything the app can see is wired up   correctly, including an optional <code>--ai</code> check that runs one real generation   through your configured AI binary.</li> </ul>"},{"location":"install-macos/#what-the-app-expects-at-runtime","title":"What the app expects at runtime","text":""},{"location":"install-macos/#database-and-config-locations","title":"Database and config locations","text":"<p>Upsight stores its data in a single SQLite file. The database path is the same on every platform:</p> <pre><code>~/.local/share/upsight/upsight.db\n</code></pre> <p><code>config.toml</code>, however, follows the macOS convention rather than Linux's:</p> <pre><code>~/Library/Application Support/upsight/config.toml\n</code></pre> <p>Set the <code>UPSIGHT_DB</code> environment variable before launching to point the app at a different database file, useful if you keep it on a synced volume:</p> <pre><code>export UPSIGHT_DB=\"$HOME/sync/upsight/upsight.db\"\n</code></pre> <p>Upsight is a local, single-user app. To use it on more than one machine, keep the database file in a directory that Syncthing replicates (<code>brew install syncthing</code> if you want it, or grab the cask). The app opens the database in WAL mode with a single writer, so only run one instance at a time against a given file; Syncthing also replicates the <code>-wal</code> and <code>-shm</code> sidecar files, so let a sync settle before opening the app on another machine. See Multi-machine sync.</p>"},{"location":"install-macos/#credential-storage","title":"Credential storage","text":"<p>Integration credentials (Salesforce, Aha, Clari, Todoist, and similar) are stored in the macOS Keychain, not in <code>config.toml</code>. The first time you save one in Settings, macOS may prompt you to allow Upsight to access the keychain. Approve it, or the app can't read the credential back on the next launch.</p>"},{"location":"install-macos/#salesforce-write-back","title":"Salesforce write-back","text":"<p>Editing posture and other fields always saves locally and queues the change. Pushing those changes back to Salesforce is off by default, because it writes to your live org. Turn it on in Settings under Integrations, Salesforce, \"Push field edits back to Salesforce\", then relaunch the app. With it off, your edits are kept and queued but nothing is sent to Salesforce.</p>"},{"location":"install-nixos/","title":"Install on NixOS","text":"<p>Upsight ships a Nix flake. This page shows how to add it to a NixOS or home-manager configuration and install the desktop app.</p> <p>The package is Linux only. It links the WebKitGTK 6.0 webview, which has no buildable Nix counterpart on macOS. On a Darwin system the package is marked broken and will not build.</p>"},{"location":"install-nixos/#add-the-flake-input","title":"Add the flake input","text":"<p>Add the repository to the <code>inputs</code> of your system flake and point it at nixpkgs so it does not pull a second copy:</p> <pre><code>{\n  inputs = {\n    nixpkgs.url = \"github:NixOS/nixpkgs/nixos-26.05\";\n\n    upsight = {\n      url = \"github:bashfulrobot/upsight\";\n      inputs.nixpkgs.follows = \"nixpkgs\";\n    };\n  };\n}\n</code></pre> <p>Upsight tracks <code>nixos-26.05</code>. Using the same nixpkgs avoids building the app and its dependencies twice.</p>"},{"location":"install-nixos/#install-via-the-overlay","title":"Install via the overlay","text":"<p>The flake exposes <code>overlays.default</code>, which adds <code>upsight</code> to your package set. Apply the overlay, then reference <code>pkgs.upsight</code> anywhere you list packages.</p> <p>In a NixOS configuration:</p> <pre><code>{ inputs, pkgs, ... }:\n{\n  nixpkgs.overlays = [ inputs.upsight.overlays.default ];\n\n  environment.systemPackages = [ pkgs.upsight ];\n}\n</code></pre>"},{"location":"install-nixos/#install-via-the-package-directly","title":"Install via the package directly","text":"<p>If you do not want the overlay, reference the package output for your system:</p> <pre><code>{ inputs, pkgs, ... }:\n{\n  environment.systemPackages = [\n    inputs.upsight.packages.${pkgs.system}.upsight\n  ];\n}\n</code></pre> <p><code>inputs.upsight.packages.${pkgs.system}.default</code> is the same derivation, so either attribute works.</p>"},{"location":"install-nixos/#install-with-home-manager","title":"Install with home-manager","text":"<p>The same two options apply in a home-manager module. With the overlay:</p> <pre><code>{ inputs, pkgs, ... }:\n{\n  nixpkgs.overlays = [ inputs.upsight.overlays.default ];\n\n  home.packages = [ pkgs.upsight ];\n}\n</code></pre> <p>Or reference the package directly:</p> <pre><code>{ inputs, pkgs, ... }:\n{\n  home.packages = [\n    inputs.upsight.packages.${pkgs.system}.upsight\n  ];\n}\n</code></pre>"},{"location":"install-nixos/#minimal-flake-example","title":"Minimal flake example","text":"<p>A complete <code>flake.nix</code> for a single NixOS host that installs Upsight:</p> <pre><code>{\n  inputs = {\n    nixpkgs.url = \"github:NixOS/nixpkgs/nixos-26.05\";\n\n    upsight = {\n      url = \"github:bashfulrobot/upsight\";\n      inputs.nixpkgs.follows = \"nixpkgs\";\n    };\n  };\n\n  outputs = { self, nixpkgs, upsight, ... }:\n    {\n      nixosConfigurations.myhost = nixpkgs.lib.nixosSystem {\n        system = \"x86_64-linux\";\n        modules = [\n          ({ pkgs, ... }: {\n            nixpkgs.overlays = [ upsight.overlays.default ];\n            environment.systemPackages = [ pkgs.upsight ];\n\n            # ... the rest of your host configuration ...\n          })\n        ];\n      };\n    };\n}\n</code></pre> <p>Build and switch the host with your usual command (for example <code>nixos-rebuild switch --flake .#myhost</code>). After the rebuild, <code>upsight</code> is on <code>PATH</code> and the app appears in your desktop launcher.</p>"},{"location":"install-nixos/#what-the-app-expects-at-runtime","title":"What the app expects at runtime","text":""},{"location":"install-nixos/#database-location","title":"Database location","text":"<p>Upsight stores its data in a single SQLite file. The default path is:</p> <pre><code>~/.local/share/upsight/upsight.db\n</code></pre> <p>Set the <code>UPSIGHT_DB</code> environment variable to point at a different file:</p> <pre><code>export UPSIGHT_DB=\"$HOME/sync/upsight/upsight.db\"\n</code></pre> <p>Upsight is a local, single-user app. To use it on more than one machine, keep the database file in a directory that Syncthing replicates. The app opens the database in WAL mode with a single writer, so only run one instance at a time against a given file. Syncthing also replicates the <code>-wal</code> and <code>-shm</code> sidecar files; let a sync settle before opening the app on another machine.</p>"},{"location":"install-nixos/#credential-storage","title":"Credential storage","text":"<p>Integration credentials (Salesforce, Aha, Clari, Todoist, and similar) are stored in the operating system keyring through libsecret. On Linux this is the Secret Service backend, which a keyring daemon such as GNOME Keyring or KWallet provides. Make sure a Secret Service provider is running in your session; otherwise credential reads and writes fail and integrations stay disabled. On macOS the same code path uses the system Keychain.</p>"},{"location":"install-nixos/#salesforce-write-back","title":"Salesforce write-back","text":"<p>Editing posture and other fields always saves locally and queues the change. Pushing those changes back to Salesforce is off by default, because it writes to your live org. Turn it on in Settings under Integrations, Salesforce, \"Push field edits back to Salesforce\", then relaunch the app. With it off, your edits are kept and queued but nothing is sent to Salesforce.</p>"},{"location":"install-nixos/#wayland-hyprland-and-other-wlroots-compositors","title":"Wayland (Hyprland and other wlroots compositors)","text":"<p>On Linux the app disables WebKit's accelerated DMA-BUF and compositing render paths (<code>WEBKIT_DISABLE_DMABUF_RENDERER</code> and <code>WEBKIT_DISABLE_COMPOSITING_MODE</code>), which otherwise crash the webview when the window is unmapped by a workspace switch. Both are set only when unset, so you can override them if your setup needs different rendering behaviour.</p>"},{"location":"meeting-summaries/","title":"Meeting summaries","text":"<p>The Meetings tab inside an account lists past and upcoming meetings. For past meetings, Upsight can generate an AI summary from the transcript or notes.</p>"},{"location":"meeting-summaries/#how-summaries-work","title":"How summaries work","text":"<p>The app passes meeting content to the <code>claude</code> CLI and receives a structured summary with key topics, decisions, and action items. The CLI must be on your <code>PATH</code> and authenticated for this to work.</p> <p>A generated summary appears in the meeting detail panel on the right side of the Meetings screen. If none has been generated yet, the panel shows the raw notes.</p> <p>Copy a summary in two formats, full Markdown or Slack Markdown, using the format buttons in the meeting detail.</p>"},{"location":"meeting-summaries/#the-upsight-summarize-cli","title":"The <code>upsight summarize</code> CLI","text":"<p>Upsight ships a companion CLI subcommand for batch-generating meeting summaries from transcripts stored on your filesystem.</p> <pre><code>upsight summarize\n</code></pre> <p>The CLI looks for transcript files in a configured folder structure and matches each folder to an Upsight account by name. It then generates and stores the summary inside Upsight.</p>"},{"location":"meeting-summaries/#folder-to-account-mapping","title":"Folder-to-account mapping","text":"<p>The CLI matches folder names to account names automatically when the names are the same (or close enough after normalization). When a folder name is an abbreviation or would match more than one account, you must add an explicit mapping.</p> <p>Add mappings in Settings under General, \"Meeting summaries.\" Each row maps a folder name (as the CLI sees it on disk) to a specific Upsight account. Folders that already match by name need no entry here.</p> <p>The folder name match is case-insensitive.</p>"},{"location":"meeting-summaries/#editing-a-summary","title":"Editing a summary","text":"<p>If a generated summary is wrong or incomplete, you can edit it directly. Open the meeting in the Meetings tab, scroll to the summary, and click the edit icon. Your edits save locally and replace the generated content.</p>"},{"location":"meeting-summaries/#marking-meetings-as-processed","title":"Marking meetings as processed","text":"<p>Once you review a meeting and act on its action items, you can mark it as processed using the toggle in the meeting detail panel. Processed meetings stay in the list but look different from pending ones.</p>"},{"location":"meeting-summaries/#logging-activity","title":"Logging activity","text":"<p>From the meeting detail you can log a CX activity tied to the meeting. This creates an activity record linked to the meeting date and the relevant contact.</p>"},{"location":"monthly-cx-update/","title":"Monthly CX update","text":"<p>The Monthly CX Update is a structured summary of an account's state that you send to the customer or your internal team on a regular cadence. Upsight tracks when each account's update is due and surfaces it as a reminder.</p>"},{"location":"monthly-cx-update/#how-the-due-date-works","title":"How the due date works","text":"<p>The reminder appears starting from a configured day of the month and stays visible until you mark the update as sent. You set the due day in Settings under General, \"Monthly CX Update.\" The default is day 1.</p> <p>The app does not enforce a strict deadline. Setting the due day to 5, for example, means the reminder appears from the 5th onward for each account that does not have a completed update for the current month.</p>"},{"location":"monthly-cx-update/#writing-the-update","title":"Writing the update","text":"<p>The CX Update composer pulls in the account's current data, including health posture, recent meetings, open action items, opportunities, and usage data. You can edit the draft before copying or sending it.</p> <p>Copy the update as Markdown or Slack Markdown using the composer's format buttons.</p>"},{"location":"monthly-cx-update/#marking-an-update-as-complete","title":"Marking an update as complete","text":"<p>Once you have sent the update, mark it as complete on the account's Overview tab. This clears the reminder for the current month. The record stays in the account history.</p>"},{"location":"monthly-cx-update/#meeting-agenda-integration","title":"Meeting agenda integration","text":"<p>The Monthly CX Update content feeds into the Agendas tab. The Agendas tab lets you build a meeting agenda from the update content, which you can copy and share before a sync meeting.</p>"},{"location":"multi-machine-sync/","title":"Multi-machine sync","text":"<p>Upsight is a local, single-machine app. To use it on more than one machine, put the database file in a directory that Syncthing replicates, then point each machine's app at that shared file.</p>"},{"location":"multi-machine-sync/#setup-overview","title":"Setup overview","text":"<ol> <li>Install Syncthing on both machines and add them to a shared folder.</li> <li>Put your Upsight database in the shared folder, or move it there.</li> <li>Set the <code>UPSIGHT_DB</code> environment variable on each machine to point at the file    in the shared folder.</li> <li>Configure <code>.stignore</code> on both machines to exclude transient SQLite files (see    below).</li> </ol>"},{"location":"multi-machine-sync/#pointing-the-app-at-the-shared-file","title":"Pointing the app at the shared file","text":"<p>Set <code>UPSIGHT_DB</code> before launching Upsight:</p> <pre><code>export UPSIGHT_DB=\"$HOME/sync/upsight/upsight.db\"\n</code></pre> <p>Put this in your shell profile or session environment so it applies on every launch. When set, the app always uses that path instead of the default.</p>"},{"location":"multi-machine-sync/#one-instance-at-a-time","title":"One instance at a time","text":"<p>Run only one instance of Upsight against a given database file at a time. The app opens the database in WAL mode with a single writer, so running two instances against the same file at once corrupts it.</p> <p>On a two-machine setup, close the app on machine A, wait for Syncthing to finish syncing (the tray icon shows progress), then open it on machine B.</p>"},{"location":"multi-machine-sync/#syncthing-stignore-patterns","title":"Syncthing .stignore patterns","text":"<p>SQLite writes several transient sidecar files Syncthing should not sync. Add these patterns to the <code>.stignore</code> file for your Upsight sync folder:</p> <pre><code>upsight.db-wal\nupsight.db-shm\nupsight.db.lock\n*.db.backup-v*\n*.pre-restore\nconfig.toml.tmp\nconfig.toml.bak\nupsight.session.tmp\n*.sync-conflict-*\n</code></pre> <p>The same list is shown in Settings under Data, Sync.</p> <p><code>upsight.db-wal</code> and <code>upsight.db-shm</code> are the WAL journal files SQLite writes while the database is open. Syncing them to another machine while the app is running would corrupt the database there. <code>upsight.db.lock</code> stops Syncthing from replicating an open lock file. <code>*.db.backup-v*</code> and <code>*.pre-restore</code> exclude Upsight's own backup and pre-restore copies so they do not bloat the sync folder. <code>config.toml.tmp</code> and <code>config.toml.bak</code> are transient config writes. <code>upsight.session.tmp</code> is a temporary session file.</p> <p><code>*.sync-conflict-*</code> suppresses Syncthing's own conflict copies. If a conflict file appears (which should not happen if you follow the one-instance rule), do not open it in Upsight. Instead, inspect both copies and decide which one to keep, then delete the other.</p>"},{"location":"multi-machine-sync/#letting-a-sync-settle","title":"Letting a sync settle","text":"<p>After closing the app on one machine, wait for Syncthing to finish syncing before opening it on another. Syncthing replicates the <code>-wal</code> and <code>-shm</code> sidecar files alongside the main database file. Opening the app mid-sync risks reading a half-transferred checkpoint.</p> <p>A short wait, 10 to 30 seconds after the Syncthing indicator goes idle, is usually enough.</p>"},{"location":"salesforce/","title":"Salesforce integration","text":"<p>Upsight reads from and writes to Salesforce through your locally installed <code>sf</code> CLI. The app never handles your Salesforce credentials directly.</p>"},{"location":"salesforce/#authentication","title":"Authentication","text":"<p>Log in once with the Salesforce CLI:</p> <pre><code>sf org login web\n</code></pre> <p>This opens a browser, authenticates against Kong's production Salesforce org, and stores the session in the CLI's own credential store. Upsight picks it up on the next sync.</p> <p>To check whether the app sees an authenticated session, go to Settings, then Integrations, then Salesforce. The status indicator shows your authenticated username or \"Not authenticated.\"</p> <p>You can also start the login flow from Settings, which shows the command and the auth-status indicator.</p>"},{"location":"salesforce/#syncing-data-from-salesforce","title":"Syncing data from Salesforce","text":"<p>Upsight pulls Salesforce data on demand. Open an account and click the sync button in the account header (the rotating-arrows icon above the tab strip) to pull the latest data.</p> <p>Reads are read-only SELECT queries against the live Kong production org.</p>"},{"location":"salesforce/#salesforce-write-back-off-by-default","title":"Salesforce write-back (off by default)","text":"<p>Write-back is the path that pushes your local edits (posture fields, health indicators, notes) back to Salesforce. It is off by default, because turning it on writes to the live Kong org.</p> <p>With write-back off:</p> <ul> <li>Edits still save locally and appear in the UI immediately.</li> <li>Changes queue in the local database.</li> <li>Nothing leaves your machine.</li> </ul> <p>To enable write-back: go to Settings, Integrations, Salesforce, and turn on \"Push field edits back to Salesforce\", then relaunch the app.</p> <p>Once enabled, a field edit pushes after a short debounce. A background drain retries any failures automatically.</p> <p>Before enabling, check the Sync Log to review any queued edits from earlier sessions. Open it by clicking \"Sync Log\" at the bottom of the left sidebar. Those queued edits push on the first launch after you enable write-back.</p>"},{"location":"salesforce/#the-sf-cli-path","title":"The <code>sf</code> CLI path","text":"<p>By default the app finds the <code>sf</code> CLI on your <code>PATH</code>. If you need to point it at a specific binary, set a custom path under Settings, Integrations, Salesforce. Toggle \"Auto\" off and enter the absolute path to the binary.</p>"},{"location":"salesforce/#cases","title":"Cases","text":"<p>Cases are pulled from Salesforce and displayed read-only. There is no case write-back; case management stays in Salesforce.</p>"},{"location":"salesforce/#jira-and-slack-links","title":"Jira and Slack links","text":"<p>If a case has a linked Jira ticket or Slack thread in Salesforce, a matching button appears on the case and opens that URL in your default browser. Hover over the button first to see which host it points to.</p>"},{"location":"salesforce/#escalation-and-staleness-indicators","title":"Escalation and staleness indicators","text":"<p>Two indicators surface on the Cases list and case detail pane, both computed locally from data already synced (nothing new to configure):</p> <ul> <li>Escalated (red activity icon): the case's Status is one of \"Escalated to   Eng,\" \"Escalated to Services,\" or \"Internal Escalation,\" or the case has a   Jira ticket linked. Closed and solved cases never show this, even if they were   escalated at some point, since the escalation is no longer active.</li> <li>Stale (amber clock icon): the case is open and its most recent comment (or   its creation date, if it has never had a comment) is more than a week old.   Closed and solved cases never show this either.</li> </ul>"},{"location":"settings/","title":"Settings","text":"<p>Settings is reachable from the bottom of the left sidebar. It is organized into six sections: General, Integrations, AI, Catalogs, Data, and About.</p>"},{"location":"settings/#general","title":"General","text":"<p>Appearance. Choose Light, Dark, or System for the color theme. System follows your OS setting. Density switches between Comfortable and Compact; Compact tightens row heights to show more data per screen.</p> <p>Monthly CX Update. Set the day of month when the CX update reminder becomes visible for accounts without a completed update. Accepts 1 through 28.</p> <p>Meeting summaries. Map folder names to Upsight accounts for the <code>upsight summarize</code> CLI. Add a row when a folder name does not match an account automatically (for abbreviations or ambiguous names). Folders that already match by name need no mapping here.</p> <p>Clipboard. Upsight detects available clipboard tools (<code>wl-copy</code>, <code>xclip</code>, <code>xsel</code>) and uses the first one it finds. To use a specific tool, select it from the dropdown. The detected or configured tool is shown next to the label.</p> <p>Notifications. Enable desktop notifications for Salesforce sync results and new Slack touchpoints.</p> <p>Keyboard shortcuts. A reference list of all keyboard shortcuts in the app.</p>"},{"location":"settings/#integrations","title":"Integrations","text":"<p>Salesforce. Shows the authentication status of your <code>sf</code> CLI session. Click \"Log in to Salesforce\" if not authenticated. You can also set a custom path to the <code>sf</code> binary if it is not on your <code>PATH</code>.</p> <p>Todoist. Enter your Todoist API token to enable task sync. Find the token in Todoist under Settings, Integrations, Developer. After saving, use \"Test\" to verify the connection. Revoke and regenerate this token from Todoist Settings if you suspect it was exposed.</p> <p>Aha! Enter your Aha! API credentials to enable feature request sync. A \"Test\" button verifies the connection. The separate \"Post comments to Aha!\" toggle gates whether Upsight can post a comment back to an Aha! idea.</p> <p>Clari Copilot. Enter your Clari Copilot API credentials to pull conversation intelligence data. Find credentials in Clari under Workspace Settings, Integrations, Clari Copilot API.</p> <p>Google Calendar. Paste your Google Calendar private ICS URL to pull upcoming meeting events. Find the URL in Google Calendar under Settings, \"Settings for my calendars,\" select your calendar, \"Integrate calendar,\" then \"Secret address in iCal format.\" Use \"Test\" to verify the sync. This URL is a permanent bearer credential. Treat it like a password and do not share it.</p> <p>Slack. Enter your Slack channel name and Client ID for Slack integration. The Slack channel is used for product updates and release touchpoints.</p> <p>Product updates retention. Sets how many days of Slack-sourced product update records to keep (default 90).</p> <p>Salesforce write-back. \"Push field edits back to Salesforce\" enables the drain that sends locally queued field changes to Salesforce. This is off by default. See Salesforce integration for the full details.</p> <p>Todoist ship events. Gates whether Upsight offers a \"create Todoist task\" button when an Aha idea reaches a shipped status in the Events feed.</p> <p>Todoist suggested tasks. Enables periodic suggestions for stale Todoist tasks linked to your accounts. Set the check interval and how many inactive days trigger a suggestion.</p>"},{"location":"settings/#ai","title":"AI","text":"<p>The AI section configures the <code>claude</code> CLI used for generating meeting summaries and other AI features. By default Upsight finds <code>claude</code> on your <code>PATH</code>. Toggle \"Auto\" off to set a custom path to the binary.</p> <p>The AI section also shows the resolved binary path after detection.</p>"},{"location":"settings/#catalogs","title":"Catalogs","text":"<p>Catalogs define the master lists for the Stack screen: Products and Infrastructure. You can add, edit, and archive catalog entries here. Each entry has a name, category, and an active flag (inactive entries are hidden from the Stack pickers but not deleted).</p> <p>The undo button restores the last deleted catalog entry.</p>"},{"location":"settings/#data","title":"Data","text":"<p>Database. Shows the current database path, file size, schema version, integrity status, and last backup timestamp. \"Compact (VACUUM)\" rewrites the database to recover space. \"Back up now\" takes an immediate snapshot.</p> <p>Automatic backups. Set how often backups run (in hours) and how many daily and weekly snapshots to keep. Changes take effect on the next launch.</p> <p>Data Maintenance. \"Backfill Win Wires from Salesforce\" re-syncs every win wire record against Salesforce to pull the authoritative close date, ACV, TCV, opportunity name, owner, and stage. Safe to run anytime.</p> <p>Import / Export. Export selected accounts to a bundle ZIP or import a bundle from another Upsight instance. See Backup and restore for details.</p> <p>Sync. Shows the <code>.stignore</code> patterns to add to your Syncthing folder config. See Multi-machine sync for details.</p>"},{"location":"settings/#about","title":"About","text":"<p>Paths. Shows the locations of the config file, database, and session file.</p> <p>Diagnostics. Shows the logs directory and the backup directory, with buttons to open the log folder and take an immediate backup.</p> <p>Privacy. Toggle the crash-report dialog. When enabled, an uncaught exception shows a redacted report you can copy and file as a GitHub issue. Nothing is sent automatically.</p> <p>Version. Shows the app version and build stack.</p>"}]}